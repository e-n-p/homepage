export interface Todo {
    id: string,
    name: string,
    description: string,
    due: Date,
}

export interface Track {
    id: string,
    intensity: string,
    pattern : string,
    colour: string,
}
